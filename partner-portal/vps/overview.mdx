---
title: "VPS Servers"
description: "Purchase, provision, and manage cloud VPS instances from Contabo and assign them to your customers."
---

## Overview

VPS (Virtual Private Server) management lets partners purchase, provision, and manage cloud VPS instances from **Contabo**, then assign them to their customers. It's a self-serve infrastructure layer within the Partner Portal.

![Knotie AI Pro Partner Portal VPS management page](/images/screenshots/vps-page-2026-06-09.png)

## Where to find it

1. Partner Portal → **VPS Servers** in the sidebar (under **INFRASTRUCTURE**)

## Purchase a VPS

1. Click **Buy VPS**
2. Select a **plan** (Cloud VPS 10–60)
3. Select a **region** (EU, US — more coming soon)
4. Optionally assign to a **customer**
5. Optionally set a **display name**
6. Click **Proceed to Payment**

![Partner Portal — VPS purchase view: plan selection cards (Cloud VPS 10–60 with vCPU, RAM, NVMe, network specs and monthly pricing) followed by a region picker (EU/US regions marked "Coming soon — pricing for this region is being finalised") and a "Proceed to Payment" button.](/images/screenshots/partner-vps-plan-region-picker.png)

### Plans available

| Plan | vCPU | RAM | NVMe Storage | Network Port |
|------|------|-----|--------------|--------------|
| Cloud VPS 10 | 4 | 8 GB | 75 GB | 200 Mbps |
| Cloud VPS 20 | 6 | 12 GB | 100 GB | 400 Mbps |
| Cloud VPS 30 | 8 | 24 GB | 200 GB | 600 Mbps |
| Cloud VPS 40 | 12 | 48 GB | 250 GB | 800 Mbps |
| Cloud VPS 50 | 16 | 64 GB | 300 GB | 1 Gbps |
| Cloud VPS 60 | 18 | 96 GB | 350 GB | 1 Gbps |

![Partner Portal — VPS plan cards showing Cloud VPS 10–60 with full specs (vCPU, RAM, NVMe storage, network port), monthly USD pricing, and the "Popular" badge on Cloud VPS 20. Same capture as above.](/images/screenshots/partner-vps-plan-region-picker.png)

## Instance states

VPS instances move through these states:

| State | Description |
|-------|-------------|
| `pending_payment` | Order placed, awaiting payment |
| `pending_provisioning` | Payment received, being set up |
| `active` | Live and operational |
| `suspended` | Paused due to non-payment or issues |
| `cancelled` | Terminated |

{/* Screenshot needed: instance cards with different status badges. Deferred 2026-06-18 — this KnoLabs AI Agency account has 0 VPS instances ("No VPS instances yet. Purchase one from the Purchase tab."); instance-card UI requires a real active instance to capture. Existing `vps-instances-nav-annotated.jpg` covers the related nav-tab view but not specific status badges. */}

## Assign to customer

Each instance can be assigned to a customer:

1. Open the **Assign to Customer** dropdown on the instance card
2. Select the customer (or "Not assigned")
3. Confirm

Unassigned instances show "Not assigned to a customer."

## View instance details

Click on an instance card to see:
- Plan specs
- Region
- Partner price
- Creation date
- Status badge

{/* Screenshot needed: instance details panel. Deferred 2026-06-18 — account has 0 VPS instances; details panel renders only for an existing instance. */}

## Credentials

For `active` instances, you can request root credentials:

1. Click **Request Credentials**
2. Credentials are sent to your email and displayed on screen
3. Password is shown in a monospace box

{/* Screenshot needed: credentials reveal panel. Deferred 2026-06-18 — requires clicking "Request Credentials" on a live active instance, which the current 0-instance account cannot provide. Existing `vps-instances-nav-annotated.jpg` shows the related nav. */}

### Regenerate SSH keys

To regenerate SSH keys for an instance:

1. Click **Regenerate SSH Keys**
2. Confirms the action
3. New keys are generated

### Fix credentials

If credentials aren't working, use the **Fix Credentials** modal:

- **Auto-fix**: Platform automatically generates fresh credentials and injects them via SSH
- **Manual**: Enter your current root password, and the platform SSHs in to install a new key

{/* Screenshot needed: Fix Credentials modal with auto/manual options. Deferred 2026-06-18 — same reason; requires an active instance. Existing `vps-reinstall-popup-annotated.jpg` covers a related modal. */}

## Reinstall with App

You can reinstall the OS with one-click apps:

### Single App mode

Select one app to install. Options include:
- Clean Ubuntu OS
- Docker + Portainer
- Domain controllers
- Other curated apps

### Package Builder mode

Select multiple apps to install together as a package:

- **Port conflict detection**: If two apps use the same port, a warning blocks installation
- **RAM estimation**: Shows total RAM required across selected apps
- **Badge indicators**: "Verified" (passed testing), "Under construction" (being fixed), "New", "Popular"

![Partner Portal — VPS Settings page App Packages section showing "New Package" modal with package name, pricing, customer selection, and Bundled Apps multi-select. This is the closest equivalent to the Reinstall-with-App Package Builder mode accessible in the current account.](/images/screenshots/partner-vps-app-package-builder.png)

### App categories

| Category | Examples |
|----------|----------|
| AI & ML | — |
| Developer Tools | — |
| Databases | — |
| Monitoring | — |
| Productivity | — |
| Security | — |
| Networking | — |

## Next steps

- [VPS App Catalog](/partner-portal/vps/app-catalog) — browse and deploy apps
- [AI Gateway](/partner-portal/ai-gateway) — manage API keys and budgets
- [Customer Management](/customer-management/onboard-customer) — assign VPS to customers
- [Partner Portal Overview](/partner-portal/overview) — explore other features
